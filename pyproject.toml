[project]
name = "daily-biorxiv"
version = "0.1.0"
description = "Daily crawl bioRxiv papers and use LLMs to summarize them"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "requests>=2.32.3",
    "python-dotenv>=1.0.1",
    "tqdm>=4.66.5",
    "PyPDF2>=3.0.1",
]
license = "Apache-2.0"
